<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/black"
            android:textSize="25dp"
            android:text="Are you sure you want to sign out ?"
            android:id="@+id/tvConfirmSignOut"/>

        <Button
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="Sign out"
            android:id="@+id/btnConfirmSignOut"
            android:layout_centerHorizontal="true"
            android:layout_below="@+id/tvConfirmSignOut"
            android:background="@drawable/white_rounded_button"/>


    </RelativeLayout>

    <ProgressBar
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerInParent="true"
        android:id="@+id/signingOutProgress"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textSize="20dp"
        android:textColor="@color/black"
        android:text="Signing out .."
        android:layout_alignBottom="@+id/signingOutProgress"
        android:layout_alignRight="@+id/signingOutProgress"
        android:layout_alignLeft="@+id/signingOutProgress"
        android:id="@+id/signingOutTv"
        android:paddingLeft="40dp"
        android:layout_marginTop="20dp"/>

</RelativeLayout>